{
  "name": "alchemists-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vc dev --listen 3000",
    "dev:vite": "vite --host 127.0.0.1 --port 3000",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vercel": "^54.2.0",
    "vite": "^8.0.13"
  }
}
