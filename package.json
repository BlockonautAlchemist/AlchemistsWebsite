{
  "name": "alchemists-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000",
    "dev:vercel": "vc dev --listen 3000",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test"
  },
  "devDependencies": {
    "vercel": "^54.2.0",
    "vite": "^8.0.13"
  }
}
